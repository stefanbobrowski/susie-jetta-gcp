steps:
  # 1️⃣ Build frontend with Node 22
  - name: 'node:22.14'
    entrypoint: npm
    args: ['ci']
    dir: 'frontend'

  - name: 'node:22.14'
    entrypoint: npm
    args: ['run', 'build']
    dir: 'frontend'

  # 2️⃣ Build backend with Node 22
  - name: 'node:22.14'
    entrypoint: npm
    args: ['ci']
    dir: 'backend'

  - name: 'node:22.14'
    entrypoint: npm
    args: ['run', 'build']
    dir: 'backend'

  # 3️⃣ Copy frontend build output into backend/public
  - name: 'ubuntu'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        mkdir -p backend/public
        cp -r frontend/dist/* backend/public/

  # 4️⃣ Deploy prebuilt backend directly (no rebuild)
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: 'gcloud'
    args:
      [
        'run', 'deploy', 'susie-jetta',
        '--source', 'backend/.',           # deploy compiled backend directly
        '--region', 'us-central1',
        '--allow-unauthenticated'
      ]

options:
  logging: GCS_ONLY

logsBucket: "gs://susie-jetta-build-logs"
